# Frontend (public-safe — exposed to browser)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# API Configuration (public-safe)
OPENMETEO_API_URL=https://api.open-meteo.com/v1/forecast
RELIEFWEB_APP_NAME=disastershield-ai

# Server-side only (NEVER expose in frontend code)
NASA_FIRMS_API_KEY=
